import profileTemplate from 'text!./profile.html'
import ProfileController from './profile.controller'

const ProfileComponent = {
  template: profileTemplate,
  controller: ProfileController
}

export default ProfileComponent
